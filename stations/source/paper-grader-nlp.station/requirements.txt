# paper-grader-nlp drives the paper-proof-grader engine, which is PURE STDLIB.
# No third-party packages are required to grade papers locally.
#
# Optional heavier NLP layer (zero-shot DeBERTa / sbert embeddings) is provided
# by separate stations (deberta-runner, sbert-embedder) and the proof-grader
# fruits-of-spirit bridge. Enable those only if you wire that layer in:
# torch
# transformers
# sentence-transformers
