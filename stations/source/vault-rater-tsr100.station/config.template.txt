# vault-rater-tsr100 — CONFIG TEMPLATE (safe to commit / share)
# ---------------------------------------------------------------
# Copy this file to config.txt and fill in your values, OR set them
# as environment variables. Precedence: ENV VAR > config.txt.
#
# config.txt is gitignored. NEVER commit, zip, or share config.txt
# once it contains a real key. Prefer the OPENAI_API_KEY env var so
# the secret never lives in this folder at all.

OPENAI_API_KEY=sk-PASTE_YOUR_KEY_HERE
BASE_URL=https://gateway.ai.cloudflare.com/v1/<CF_ACCOUNT_ID>/<GATEWAY_NAME>/openai
MODEL=gpt-4o
TEMPERATURE=0.3
MAX_TOKENS=2000
