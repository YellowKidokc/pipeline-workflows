Extract discrete claims and map relevant claims to the one admitted root axiom, God Is, or explicitly named supporting nodes. Supporting nodes, definitions, and conditional derivations are not additional axioms. Do not require every paper to restate the root, invent nodes, or equate mapping with proof.
Return one JSON object with claims (statement, category, source anchor, confidence), mappings (claim, axiom or node, relation, warrant), gaps, contradictions, and confidence (0 to 1). Preserve uncertainty and distinguish admitted premises from demonstrated consequences.
Document:
{{INPUT}}
