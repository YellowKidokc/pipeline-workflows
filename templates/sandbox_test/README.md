# Sandbox Test Packet

Phase 0 safety packet for `sandbox-file-intake`. The test harness copies sample
files into `INPUT/`, runs the orchestrator in dry-run mode, and verifies state
tracking without touching originals.
