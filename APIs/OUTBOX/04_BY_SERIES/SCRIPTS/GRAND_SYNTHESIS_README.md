Adapted from the original Evidence grand synthesizer. Run BUILD_SERIES_READER first. Uses only the named series and requires all manifest members. Reads full chapter text; refuses oversized input instead of truncating. DeepSeek default; --provider openrouter remains available. Requires requests. No API run performed during installation. Legacy source retained for comparison, not active use. Output is a reviewed-series draft, not formal verification.
