# NLP capabilities
Portable implementations are separated by capability in `APIs/workbench/nlp.py`: cleanup, extraction, classification, chunking, and indexing. Subdirectories are stable extension homes for future implementations.
